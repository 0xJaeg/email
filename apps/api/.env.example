# Server
PORT=3001

# Supabase (backend reads secret key; URL is the same as the publishable one)
NEXT_PUBLIC_SUPABASE_URL=
SUPABASE_SECRET_KEY=

# Redis (for enqueueing to BullMQ)
REDIS_URL=redis://localhost:6379

# Agent Mail (used in slice B)
AGENT_MAIL_WEBHOOK_SECRET=
